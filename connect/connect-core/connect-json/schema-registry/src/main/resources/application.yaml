server:
  port: 8080

miranum.schema.registry.url:

spring:
  datasource:
    url:
    driverClassName:
    username:
    password:
  jpa:
    database-platform:
  flyway:
    enabled: true
    locations: classpath:db
